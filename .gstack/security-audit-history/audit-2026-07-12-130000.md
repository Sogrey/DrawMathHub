# DrawMathHub 安全审计报告

## Meta
- 审计模式: Comprehensive (全量审计)
- 日期: 2026-07-12T13:00:00+08:00
- 范围: DrawMathHub 全量代码库 (F:\woekspace\rust\DrawMathHub)
- 执行阶段: 14/14
- 审计人: gstack-security-officer (CSO)

## 核心结论

**Conditional Go (有条件放行)** — 项目整体安全基线良好（无密钥泄露、无 XSS、无已知依赖漏洞、CI/CD 干净），但密码使用无盐 SHA-256 哈希存在真实风险，面向小学生群体应提高标准，建议在首个迭代内修复后上线。

## 严重度分布

| 严重度 | 数量 |
|--------|------|
| 🔴 Critical | 1 |
| 🟠 High | 2 |
| 🟡 Medium | 2 |
| 🟢 Low | 5 |
| ℹ️ Info | 3 |
| **合计** | **13** |

## 密钥/敏感信息泄露检查结论

**✅ 无泄露。** 具体检查结果：

1. `.env` 文件内容仅为 `VITE_BASE_PATH=/DrawMathHub/`（部署子路径配置），不含任何 API Key、Token、密码或凭证。
2. `.env.example` 与 `.env` 内容一致，为纯路径配置模板。
3. `git log --all --full-history -- .env` 确认 `.env` 仅在初始提交 (65656bb) 中添加一次，内容始终为路径配置，历史中从未包含密钥。
4. `git diff .env` 无改动 — `.env` 文件未被修改（team-lead 提到的 ".env 被修改" 实际未发生，git status 中 .env 不在修改列表内）。
5. 全量代码搜索 `api_key|secret|token|credential|private_key|BEGIN RSA` 未发现硬编码密钥。
6. 全量代码搜索 IP 地址模式未发现硬编码 IP。

**注意**: `.gitignore` 未忽略 `.env` 文件本身（仅忽略 `.env.local` 和 `.env.*.local`）。当前内容无害，但此模式存在未来误提交密钥的风险。

## STRIDE 威胁建模

| 组件 | 威胁类型 | 风险描述 | 严重度 | 缓解建议 |
|------|----------|----------|--------|----------|
| 登录认证 | Spoofing (伪造) | 任何人可创建任意昵称，无服务端身份验证。由于数据存储在本地 IndexedDB，影响范围限于同一浏览器 | 🟡 Medium | 纯前端架构固有限制，可接受。如需增强可考虑后续引入后端认证 |
| 登录认证 | Information Disclosure (信息泄露) | 用户完整对象（含 passwordHash）以 JSON 存入 sessionStorage，任何同源 JS 可读取 | 🟠 High | sessionStorage 中仅存 nickname，不存 passwordHash |
| 登录认证 | Denial of Service (拒绝服务) | 密码输入无次数限制、无锁定机制，可被暴力尝试 | 🟢 Low | 纯前端场景影响有限，可加客户端延时重试 |
| 数据存储 (IndexedDB) | Tampering (篡改) | IndexedDB 数据可被浏览器开发者工具或 JS 直接读写修改 | 🟡 Medium | 纯前端固有限制。密码哈希应使用带盐 PBKDF2 增加篡改成本 |
| 数据存储 (IndexedDB) | Information Disclosure (信息泄露) | 密码哈希以明文 SHA-256 存储在 IndexedDB，无盐保护，易被彩虹表破解 | 🔴 Critical | 改用 PBKDF2 + 随机盐，迭代次数 ≥ 100000 |
| 数据存储 (IndexedDB) | Elevation of Privilege (权限提升) | 用户可通过直接操作 IndexedDB 读取/修改其他用户的学习进度 | 🟢 Low | 纯前端固有限制，数据敏感度低（学习记录） |
| 视频播放 | Tampering (篡改) | 视频 URL 来自 manifest.json，manifest 中 `segment.file` 字段未做路径校验，理论上可构造路径穿越 | 🟢 Low | 静态文件同源服务，GitHub Pages 不执行服务端文件读取，实际风险极低 |
| 题目数据 | Tampering (篡改) | 题目 JSON 为静态文件，无完整性校验（无 SRI/hash） | 🟢 Low | 同源静态文件，篡改需仓库写入权限，可接受 |
| 题目数据 | Repudiation (抵赖) | 答题提交始终标记为正确（`isCorrect = true` 硬编码），无法真实记录学习效果 | 🟢 Low | 修复答题校验逻辑（功能缺陷而非安全问题） |

## OWASP Top 10 检查表

| 编号 | 类别 | 是否涉及 | 发现 | 严重度 |
|------|------|----------|------|--------|
| A01 | 失效访问控制 | ✅ 是 | 路由守卫为纯客户端实现，可通过修改 sessionStorage 绕过。但数据全本地存储，影响有限 | 🟡 Medium |
| A02 | 加密失败 | ✅ 是 | 密码使用无盐 SHA-256 哈希（`indexedDB.ts:64-71`），易被彩虹表破解。passwordHash 存入 sessionStorage | 🔴 Critical |
| A03 | 注入 | ❌ 否 | 全量搜索未发现 `v-html`、`innerHTML`、`document.write`、`eval()` 使用。Vue 默认模板转义生效 | ℹ️ Info |
| A04 | 不安全设计 | ✅ 是 | 匿名登录 + 可选密码设计可接受，但密码保护机制（哈希算法）不达标。答题校验逻辑缺失 | 🟢 Low |
| A05 | 安全配置错误 | ✅ 是 | 无 CSP (Content Security Policy) 头；`.gitignore` 未忽略 `.env`；构建未显式禁用 sourcemap | 🟠 High |
| A06 | 脆弱依赖 | ❌ 否 | `npm audit` 报告 0 漏洞（184 个依赖全通过） | ℹ️ Info |
| A07 | 认证失败 | ✅ 是 | 无暴力破解防护；session 恢复依赖 sessionStorage 可被伪造；密码比较使用 `===`（非常量时间比较） | 🟢 Low |
| A08 | 数据完整性 | ✅ 是 | 题目数据 JSON 无完整性校验；答题结果始终为正确（逻辑缺陷） | 🟢 Low |
| A09 | 日志监控失败 | ⚠️ N/A | 纯前端无后端，无审计日志。此为架构固有限制，非缺陷 | ℹ️ Info |
| A10 | SSRF | ❌ 否 | 纯前端应用，无服务端请求代理。fetch 调用仅针对同源静态资源 | ℹ️ Info |

## 审查发现表

| # | 严重度 | 类别 | 位置 (文件:行) | 问题描述 | 修复建议 |
|---|--------|------|----------------|----------|----------|
| F-001 | 🔴 Critical | A02 加密失败 | `src/db/indexedDB.ts:64-71` | 密码使用无盐 SHA-256 哈希。相同密码始终产生相同哈希，易被彩虹表破解；SHA-256 计算速度快，GPU 暴力破解效率高 | 改用 Web Crypto API 的 `crypto.subtle.deriveBits` 实现 PBKDF2，使用随机盐（≥16字节），迭代次数 ≥ 100000 |
| F-002 | 🟠 High | A02 加密失败 | `src/stores/userStore.ts:49,71` | `sessionStorage.setItem(SESSION_KEY, JSON.stringify(user))` 将完整用户对象（含 passwordHash）序列化存入 sessionStorage，任何同源 JS 可读取 | sessionStorage 中仅存储 `{ nickname }` 最小信息，不存 passwordHash。恢复 session 时从 IndexedDB 重新加载用户数据 |
| F-003 | 🟠 High | A05 配置错误 | `index.html:1-15` | 无 Content Security Policy。如未来引入 XSS 向量，无纵深防御。外部资源 (Google Fonts) 无限制加载 | 在 `index.html` `<head>` 中添加 CSP meta 标签：`<meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; media-src 'self'; img-src 'self' data:;">` |
| F-004 | 🟡 Medium | A01 访问控制 | `src/router/index.ts:30-44` | 路由守卫为纯客户端实现，攻击者可通过浏览器控制台修改 `sessionStorage` 或 Pinia store 绕过认证 | 纯前端架构固有限制。建议在路由守卫中增加从 IndexedDB 验证用户真实存在的逻辑，提高绕过成本。长期需引入后端认证 |
| F-005 | 🟡 Medium | A05 配置错误 | `.gitignore:6-9` | `.gitignore` 未忽略 `.env` 文件本身。当前内容仅为路径配置无密钥，但此模式存在未来误提交密钥的风险 | 在 `.gitignore` 中添加 `.env` 行，将 `.env` 从版本控制移除（`git rm --cached .env`），保留 `.env.example` 作为模板 |
| F-006 | 🟢 Low | A07 认证失败 | `src/stores/userStore.ts:42`, `src/stores/userStore.ts:103` | 密码哈希比较使用 `===`（非常量时间比较），理论上存在时序攻击可能 | 使用 `crypto.subtle.timingSafeEqual` 或自实现常量时间比较函数。实际风险极低（纯前端场景） |
| F-007 | 🟢 Low | A01 访问控制 | `src/router/index.ts:39-40`, `src/views/Login.vue:133-138` | redirect 参数仅校验 `startsWith('/')`，允许 `//evil.com` 协议相对 URL。Vue Router 通常将其视为内部路径，实际风险低 | 增加 `!redirect.startsWith('//')` 校验，拒绝协议相对 URL |
| F-008 | 🟢 Low | A04 不安全设计 | `src/views/Problem.vue:370-371` | `submitAnswer()` 始终设置 `isCorrect.value = true`，未实际校验答案。导致学习进度数据失真 | 实现答案校验逻辑，与 `currentExercise` 的正确答案比较。此为功能缺陷，非直接安全问题 |
| F-009 | 🟢 Low | A05 配置错误 | `vite.config.ts:17-25` | 未显式设置 `build.sourcemap: false`。Vite 默认不生成 sourcemap，但显式设置可防止配置继承时的意外开启 | 在 vite.config.ts 的 return 对象中添加 `build: { sourcemap: false }` |
| F-010 | 🟢 Low | A08 数据完整性 | `src/data/problems.ts:67-76`, `src/data/videoAssets.ts:16-18` | 题目数据 JSON 和视频 manifest 无完整性校验（无 SRI hash）。manifest 中 `segment.file` 字段未做路径穿越校验 | 静态同源文件风险低。可对 `segment.file` 添加 `../` 检测过滤。长期可引入 SRI 校验 |
| F-011 | 🟢 Low | A05 配置错误 | `scripts/vite-plugin-asset-404.ts:16` | `resolvePublicFile` 使用 `path.join(root, 'public', decodeURIComponent(relative))` 存在路径穿越风险。仅影响开发服务器，且受 STATIC_ASSET 扩展名限制 | 添加 `path.resolve` 后校验结果是否在 `path.join(root, 'public')` 目录内。生产环境不受影响 |
| F-012 | ℹ️ Info | A03 注入 | 全量 src/ | 未发现 XSS 向量。Vue 默认模板转义生效，无 `v-html`/`innerHTML`/`eval` 使用 | 保持现状，代码审查中持续禁止 `v-html` 使用 |
| F-013 | ℹ️ Info | CI/CD | `.github/workflows/deploy.yml` | CI/CD 管道安全良好：使用 `actions/checkout@v4`、`npm ci`、权限最小化（contents:read, pages:write）、仅 main 分支部署、无 `pull_request_target` | 保持现状 |

## Top 3 必修安全项

### 1. [P0] 密码哈希改用 PBKDF2 + 随机盐 (F-001)
**位置**: `src/db/indexedDB.ts:64-71`
**当前代码**:
```typescript
export async function hashPassword(password: string): Promise<string> {
  const encoder = new TextEncoder()
  const data = encoder.encode(password)
  const hash = await crypto.subtle.digest('SHA-256', data)
  return Array.from(new Uint8Array(hash))
    .map(b => b.toString(16).padStart(2, '0'))
    .join('')
}
```
**修复方案**:
```typescript
export async function hashPassword(password: string): Promise<string> {
  const encoder = new TextEncoder()
  const salt = crypto.getRandomValues(new Uint8Array(16))
  const keyMaterial = await crypto.subtle.importKey(
    'raw', encoder.encode(password), 'PBKDF2', false, ['deriveBits']
  )
  const derivedBits = await crypto.subtle.deriveBits(
    { name: 'PBKDF2', salt, iterations: 100000, hash: 'SHA-256' },
    keyMaterial, 256
  )
  const saltHex = Array.from(salt).map(b => b.toString(16).padStart(2, '0')).join('')
  const hashHex = Array.from(new Uint8Array(derivedBits)).map(b => b.toString(16).padStart(2, '0')).join('')
  return `pbkdf2$100000$${saltHex}$${hashHex}`
}
```
同时需修改密码验证逻辑以解析盐值。

### 2. [P1] sessionStorage 不存储 passwordHash (F-002)
**位置**: `src/stores/userStore.ts:49,71,120-129`
**修复方案**: sessionStorage 中仅存 `JSON.stringify({ nickname: user.nickname })`，`restoreSession()` 时根据 nickname 从 IndexedDB 重新加载用户数据。

### 3. [P1] 添加 Content Security Policy (F-003)
**位置**: `index.html`
**修复方案**: 在 `<head>` 中添加:
```html
<meta http-equiv="Content-Security-Policy" content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; media-src 'self'; img-src 'self' data:; connect-src 'self';">
```

## 修复路线图

| 优先级 | 发现编号 | 修复内容 | 建议时间 |
|--------|----------|----------|----------|
| P0 (立即) | F-001 | 密码哈希改用 PBKDF2 + 盐 | 上线前 |
| P1 (本迭代) | F-002 | sessionStorage 不存 passwordHash | 上线前/同 PR |
| P1 (本迭代) | F-003 | 添加 CSP meta 标签 | 上线前/同 PR |
| P2 (下迭代) | F-005 | .gitignore 添加 .env 忽略 | 下次提交 |
| P2 (下迭代) | F-004 | 路由守卫增强验证 | 下迭代 |
| P2 (下迭代) | F-007 | redirect 校验增强 | 下迭代 |
| P2 (下迭代) | F-009 | 显式禁用 sourcemap | 下迭代 |
| P3 (待办) | F-006 | 常量时间密码比较 | 待办 |
| P3 (待办) | F-008 | 修复答题校验逻辑 | 待办 |
| P3 (待办) | F-010 | 路径穿越过滤 | 待办 |
| P3 (待办) | F-011 | 开发服务器路径校验 | 待办 |

## 安全态势评分

- Critical: 1
- High: 2
- Medium: 2
- Low: 5
- Info: 3
- **Overall: B** (良好，存在需修复的安全问题但无阻塞性漏洞)

## 趋势对比

首次审计，无历史基线可对比。
